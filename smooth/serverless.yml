service: smooth
frameworkVersion: '3'

provider:
  name: aws
  stage: dev 
  region: ca-central-1

plugins:
  - serverless-sync-s3buckets
resources:
  Resources:
    SmoothOperatorBucket:
      Type: AWS::S3::Bucket
      Properties:
        BucketName: batch2024-smooth
        WebsiteConfiguration:
          IndexDocument: index.html
          ErrorDocument: index.html
        OwnershipControls:
          Rules:
            - ObjectOwnership: ObjectWriter
        PublicAccessBlockConfiguration:
          BlockPublicAcls: false
          BlockPublicPolicy: false
          IgnorePublicAcls: false
          RestrictPublicBuckets: false
    SmoothOperatorBucketPolicy:
      Type: AWS::S3::BucketPolicy
      DependsOn: SmoothOperatorBucket  
      Properties:
        Bucket: 
          Ref: SmoothOperatorBucket
        PolicyDocument:
          Statement:
            - Sid: PublicReadGetObject
              Effect: Allow
              Principal: "*"
              Action:
                - s3:GetObject
              Resource: 
                Fn::Join:
                  ["", ["arn:aws:s3:::", { "Ref": SmoothOperatorBucket }, "/*"]]

    myDistribution:
      Type: AWS::CloudFront::Distribution
      Properties:
        DistributionConfig:
          Enabled: true
          DefaultRootObject: /index.html
          Origins:
            - Id: myS3Origin
              DomainName: !GetAtt SmoothOperatorBucket.RegionalDomainName
              S3OriginConfig:
                OriginAccessIdentity: ''
          DefaultCacheBehavior:
            AllowedMethods:
              - GET
              - HEAD
              - OPTIONS
            MinTTL: 0
            MaxTTL: 0
            DefaultTTL: 0
            TargetOriginId: myS3Origin
            ForwardedValues:
              QueryString: true
              Cookies:
                Forward: all
            ViewerProtocolPolicy: redirect-to-https
          CacheBehaviors:
            - PathPattern: static/*
              AllowedMethods:
                - GET
                - HEAD
                - OPTIONS
              TargetOriginId: myS3Origin
              ForwardedValues:
                QueryString: false
              ViewerProtocolPolicy: https-only
              MinTTL: 0
              MaxTTL: 6
              DefaultTTL: 3
          CustomErrorResponses:
            - ErrorCachingMinTTL: 0
              ErrorCode: 403
              ResponseCode: 200
              ResponsePagePath: /index.html

custom:
  syncS3Buckets:
    - bucketRef: SmoothOperatorBucket
      localDir: dist/final-ui